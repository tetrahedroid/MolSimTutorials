# MolSimTutorials

「学生用資料 分子シミュレーション研究の手引き」の書換えを試みます。

* セクションごとにファイルを分けます。

## VSCodeで読む方法

1. VScodeに以下の拡張機能を追加する。
   * Markdown Preview Enhancer (CSSのカスタマイズ)
   * Markdown footnotes (footnotesの利用)
2. 拡張子`.md`のファイルを開いたら、`Command-K v`を押して、MarkDownプレビューを表示する。(もしかしたらこれにも拡張機能が要るかも)